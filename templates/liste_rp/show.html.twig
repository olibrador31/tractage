{% extends 'base.html.twig' %}

{% block title %}ListeRp{% endblock %}

{% block body %}
    <h1>ListeRp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_rp.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ liste_rp.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ liste_rp.prenom }}</td>
            </tr>
            <tr>
                <th>Entitee</th>
                <td>{{ liste_rp.entitee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liste_rp_index') }}">back to list</a>

    <a href="{{ path('app_liste_rp_edit', {'id': liste_rp.id}) }}">edit</a>

    {{ include('liste_rp/_delete_form.html.twig') }}
{% endblock %}
